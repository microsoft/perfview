{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }

    /**
      TODO: lt72

      add here the path to the 'HOME' directory where the first file tree is served from 
      also the HOME should serve as the base location where to store teh symbols (i.e. symbols folder)
     **/

  }
}
